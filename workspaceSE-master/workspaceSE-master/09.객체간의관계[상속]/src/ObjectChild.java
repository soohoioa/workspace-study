/*
5. 사용자정의 클래스들도 Object 클래스라는 최상위클래스를
    상속  받아야한다. 
 */
public class ObjectChild  /* extends Object */{
	/*
	 * extens Object 는 생략가능
	 * 
	 * << Object 클래스 >>
	 *   - Class Object is the root of the class hierarchy. 
	 *   - Every class has Object as a superclass.
	 */
}
